<% provide(:title, "スポットを探す") %>
<div class="flex flex-col items-center py-12">
  <h1 class="font-bold text-2xl text-gray-700">スポットを探す</h1>
  <p class="mt-3 font-semibold text-xl rounded bg-fields p-5">行き先を探そう!</p>
  <div class="my-3">
    <%= render 'search_form' %>
  </div>
  <%= render 'shared/spot' %>
</div>
