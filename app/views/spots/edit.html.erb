<% provide(:title, "スポットを編集する") %>
<div class="py-12 flex flex-col justify-between items-center w-full max-w-md mx-auto">
  <h1 class="font-bold text-2xl text-gray-700 text-center mb-8 flex items-center">
    <%= spot_icon %>
    スポットを編集する
  </h1>
  <%= render 'form', spot: @spot %>
</div>