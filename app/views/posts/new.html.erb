<% provide(:title, "投稿する") %>
<div class="py-12 flex flex-col justify-between items-center w-80 md:w-full max-w-lg mx-auto">
  <h1 class="font-semibold text-2xl text-gray-700 mt-10">
    <%= @post.spot.spot_name %>
  </h1>
  <%= render 'form', post: @post, spot: @spot %>
</div>